# Repository-local environment for all 21 methods.
#
# Python 3.11 is the supported interpreter.  These versions deliberately use a
# modern common scientific stack instead of installing the mutually
# incompatible historical lock files bundled with the upstream baselines.
numpy==1.26.4
pandas==2.2.3
scipy==1.12.0
scikit-learn==1.5.2
PyYAML==6.0.2
joblib==1.4.2
matplotlib==3.9.2
plotly==5.24.1
tqdm==4.67.1

# Evolutionary optimization and legacy surrogate implementations.
pymoo==0.6.1.6
GPy==1.13.2
statsmodels==0.14.4
pyDOE==0.3.8
pyDOE2==1.3.0
optproblems==1.3
graphviz==0.20.3
diversipy==0.8

# Neural, Bayesian, MOBO, and generative methods.
torch==2.2.2
botorch==0.10.0
gpytorch==1.11
pyro-ppl==1.9.1

# Molecule's official oracle and pretrained property models.
rdkit==2023.9.4
chemprop==1.6.1

# AutoGluon supplies quantile regression, XGBoost, and the weighted ensemble.
autogluon.tabular==1.2.0
xgboost==2.1.3

# The TabPFN method uses the hosted-client API and additionally needs a token.
tabpfn-client>=0.1,<1
